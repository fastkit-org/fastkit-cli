[project]
name = "fastkit-cli"
version = "0.1.0"
description = "CLI for fastkit-core"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastkit-core>=0.3.5",
    "jinja2>=3.1.6",
    "typer>=0.24.1",
]

[project.scripts]
fastkit = "fastkit_cli.main:app"
